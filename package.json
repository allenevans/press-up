{
  "name": "press-up",
  "version": "0.0.1",
  "description": "Containerised wordpress and mysql with AWS S3 backup and restore",
  "author": "Allen Evans",
  "license": "SEE LICENSE in LICENSE.md",
  "scripts": {
    "build:mysql": "cd src/mysql; docker build -f mysql.dockerfile -t allenevans/press-up-mysql .",
    "build:wordpress": "cd src/wordpress; docker build -f wordpress.dockerfile -t allenevans/press-up-wordpress .",
    "clean": "docker container prune -f",
    "start:mysql": "docker run -it --rm -p 3306:3306 --env-file ./.env --init --name press-up-mysql allenevans/press-up-mysql",
    "start:wordpress": "docker run -it --rm -p 8888:80 --env-file ./.env --init --name press-up-wordpress allenevans/press-up-wordpress"
  }
}
